package PaaS::Monitoring;

use PaaS;
use Exporter qw(import);

our $VERSION = '0.1';
our @EXPORT = qw();

sub monitor_printers {
    # Use IPPtool to get state of printers
    # Use IPPtool to get queue
        # Change jobstate accordingly
}
